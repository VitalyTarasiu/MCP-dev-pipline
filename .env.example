# =============================================================
# MCP Development Pipeline - Environment Configuration
# =============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Or just run  python main.py  — it will guide you through
# browser-based SSO setup for Jira and GitHub.
# =============================================================

# --- OpenAI API ---
OPENAI_API_KEY=your-openai-api-key

# --- Model Configuration ---
# Developer agent (cheap, fast model)
DEVELOPER_MODEL=gpt-4o-mini
# Architect / reviewer agent (premium, high-quality model)
ARCHITECT_MODEL=gpt-4o
# Product Manager agent
PM_MODEL=gpt-4o-mini
# Selector model (used internally for routing, keep cheap)
SELECTOR_MODEL=gpt-4o-mini

# --- Jira Configuration ---
# Token is created via browser SSO at:
#   https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_URL=https://think-up.atlassian.net
JIRA_USER=vitaly.tarasiuk@thinkup.global
JIRA_API_TOKEN=
JIRA_PROJECT_KEY=TUP

# --- GitHub Configuration ---
# Token is created via browser SSO at:
#   https://github.com/settings/tokens/new?scopes=repo
GITHUB_TOKEN=
GITHUB_REPO=thinkup-global/api-controller
BASE_BRANCH=dev
